import 'package:flutter/material.dart';

/// Root navigator key — lets app-level overlays (the floating video window)
/// push routes from outside the Navigator subtree.
final rootNavigatorKey = GlobalKey<NavigatorState>();
